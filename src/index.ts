/**
 * Entry point for @ycforge/ycsf-nestjs-connector.
 *
 * Intentionally empty during bootstrap: exports are added here only when a
 * public API is implemented deliberately (see AGENTS.md sections 18 and 26).
 */
export {};
